// api.js - Define your API endpoints or constants here
const API_BASE_URL = 'https://your-api-url.com';

export const API_ROUTES = {
  PARTS: `${API_BASE_URL}/parts`,
  // Add more API routes as needed
};
